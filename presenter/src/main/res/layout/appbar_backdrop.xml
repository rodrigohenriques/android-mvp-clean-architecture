<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_collapseMode="parallax"
    android:background="@color/appbar_backdrop_background"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/season_header"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:fitsSystemWindows="true"
        android:scaleType="centerCrop"
        android:src="@drawable/season_header_placeholder" />

    <ImageView
        android:id="@+id/season_picture"
        android:layout_width="120dp"
        android:layout_height="140dp"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="130dp"
        android:scaleType="centerCrop"
        android:src="@drawable/season_picture_placeholder" />

    <TextView android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/episodes"
        android:layout_alignParentBottom="true"
        style="@style/TextAppearance.AppCompat.Subhead"
        android:layout_margin="20dp" />

    <TextView
        android:id="@+id/season_rating"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/season_rating"
        android:text="@string/default_rating"
        android:layout_below="@+id/season_header"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="20dp"
        android:gravity="center"
        android:textSize="20sp"
        android:padding="4dp"
        android:textColor="@color/rating_color"/>
</RelativeLayout>